{% extends 'base.html.twig' %}

{% block title %}Chamber{% endblock %}

{% block body %}
    <h1>Chamber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chamber.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ chamber.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ chamber.description }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ chamber.price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chamber_index') }}">back to list</a>

    <a href="{{ path('app_chamber_edit', {'id': chamber.id}) }}">edit</a>

    {{ include('chamber/_delete_form.html.twig') }}
{% endblock %}
